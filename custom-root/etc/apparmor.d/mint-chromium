abi <abi/4.0>,

include <tunables/global>

profile mint-chromium /usr/lib/chromium/chromium flags=(unconfined) {
  userns,

  # Site-specific additions and overrides. See local/README for details.
  include if exists <local/chromium>
}

